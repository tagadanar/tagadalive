include('auto');

init()
debug("init ok")

// Use tagadalive AI
debug("combo search...")
Combo combo = AI.getPotentialCombo()
debug("combo: " + combo)

if(combo) {
    debug("playing " + count(combo.actions))
    combo.play()
} else {
    debugE("combo search failed")
}


Benchmark.display()


