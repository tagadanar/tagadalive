include('auto');

init()

// Run MCTS
Combo combo = AI.getMCTSCombo()

// Summary output
debug("T" + getTurn() + ": " + count(combo.actions) + " actions, score=" + round(combo.getScore()!))

// Play the combo
combo.play()

Benchmark.display()
