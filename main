include('auto');

init()

// Get best combo using selected algorithm (PTS, MCTS, or Hybrid)
// Change AI.mode in AI/AI to switch: MODE_PTS, MODE_MCTS, MODE_HYBRID
Combo combo = AI.getCombo()

// Play the combo
combo.play()

// Output debug data
// display()     -> JSON format for Tampermonkey (recommended)
// displayText() -> Human-readable format
// displayAll()  -> Both formats
Benchmark.display()
